name: Build
on: push
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build with Makefile
        run: make
      - name: Upload dylib
        uses: actions/upload-artifact@v5
        with:
          name: ios-dylib
          path: libtest.dylib
